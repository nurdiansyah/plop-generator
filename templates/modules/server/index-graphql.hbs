/* -- APPEND -- */
import { create{{ pascalCase model }}Schema } from "./{{ pascalCase model }}schema";

export const register{{ pascalCase moduleName }}Schema = () => {
  const {{ camelCase model }}Schema = create{{ pascalCase model }}Schema();
  return {
    typedefs: `
      {{#preCurly (camelCase model) }}{{/preCurly}}Schema.typeDef}
    `,
    resolvers: [
      {{ camelCase model }}Schema.resolvers
    ]
  };
};
